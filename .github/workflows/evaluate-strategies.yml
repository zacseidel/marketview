name: Evaluate Strategies

on:
  workflow_dispatch:  # manual only — options evaluation removed

jobs:
  evaluate-strategies:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Options strategy evaluation has been removed from marketview."
